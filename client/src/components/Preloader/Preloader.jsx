import React from "react";
import { Box, CircularProgress } from '@mui/material';

const Preloader = () => <Box sx={{display: 'flex', justifyContent: 'center'}}><CircularProgress/></Box>

export default Preloader;
